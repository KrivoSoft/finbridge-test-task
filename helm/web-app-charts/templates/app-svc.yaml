apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-app-charts.fullname" . }}
  labels:
    {{- include "web-app-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-app-charts.selectorLabels" . | nindent 4 }}
